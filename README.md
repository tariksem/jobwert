# JobWert

JobWert compares a current job with a new offer using estimated 2026 German payroll deductions plus housing, commuting and other recurring costs.

## Commands

```bash
npm install
npm run check
npm run build
```

The project uses Next.js static export and produces `out/`.
